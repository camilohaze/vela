[package]
name = "vela-cli"
version = "0.1.0"
edition = "2021"
authors = ["Vela Team"]
license = "MIT OR Apache-2.0"
description = "Vela language command line interface"

[dependencies]
vela-tooling = { path = "../tooling" }
clap = { version = "4.5", features = ["derive", "color"] }